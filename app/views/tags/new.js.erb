<div class="images">
  <% @images.each do |image| %>
    <%= link_to image_tag(image), "#", :class => "trigger" unless image == "#" %>
  <% end -%>
</div>

<div class="description"><%= @description %></div>