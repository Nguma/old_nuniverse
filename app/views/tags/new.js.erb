<h2>Great! You are about to create a nuniverse for <span style="color:#39F"><%= @tag.label %></span>.</h2>
<p>All we need to know is what is <%= @tag.label %>. Add as many tags as needed. Be aware that these tags are public, meaning that they will be seen by anyone, and should apply to anyone. (If you plan to tag it with something more personal, there will be a section for it.)</p>
  <%= render :partial => "/tags/box" , :locals => {:selecteds => @tag.tags, :suggesteds => ["person","character","topic","band","company","album","restaurant","museum"] }%>
 
  <div class="response"></div>
  <div class="delimiter" style="padding:5px 0 0 0 ">&nbsp;</div>
    <%= hidden_field_tag "input", @tag.label %>
    <%= hidden_field_tag "tags", "" , :class => "tags"%>
    <%= text_field_tag "tag", "" , :class => "input", :id => "input_new_tag", :mode => "tag"%>
    <%= link_to image_tag("/images/icons/plus.png", :style => "position:relative;height:30px"), "#", :class => "trigger", :mode => "tag" %>
    <%= hidden_field_tag :subject, @tag.id %>
    <%= link_to(image_tag("/images/icons/save_changes_btn.png"), create_tag_url , :class => "trigger", :mode => "create") %>
    <%= link_to(image_tag("/images/icons/cancel_btn.png"), "#", :class => "trigger") %>
