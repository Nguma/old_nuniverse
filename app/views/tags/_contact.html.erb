
<%= thumbnail_tag(element) %>
<div class="tag">Contact</div>
<h2><%= visit_url(element) %></h2>
<p><%= element.connections.sphinx('email|tel|address|twitter ', :match_mode => :extended).paginate(:page => 1, :per_page => 3).collect {|c| c.subject.body}.join(' | ') rescue nil%></p>