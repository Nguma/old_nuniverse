<%#= image_tag('') %> 
  
  <% Tagging.with_path(path, nil).with_kind_like('image').find(:all).each do |p| -%>
    <%= image_tag(p.object.image.public_filename) %>
  <% end -%>
<%= subject.description %>

