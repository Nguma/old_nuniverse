<% nuniverse(:selected => "My nuniverse") do -%>
  <%= list_options(:items => @items) %>
  <% if @items.empty? -%>
    <%= empty_box %>
  <% else -%>
      <% if @mode == "list" %>
        <div class="column large" id="item_list" >
         
          
          <% @items.each_with_index do |item, i| -%>
             <%= render_item_box(item, :kind => @list.label) %>
          <% end -%>
          
        </div>
        <div class="column small" id="small-column">
          <%= ad_box %>
          <%= map_box(@list)%>
          <%= contributors_box(:source => @list) %>
        </div>
      <% else -%>
        <%= columnize([ ad_box, boxes_for(@items, :source => @list)].flatten)%>
      <% end -%>
        
  <% end -%>

  <% content_for :nav do -%>
    
   
    <%= perspectives(:source => @list, :selected => @perspective) %>
    
    <%= add_new_button(@list) %>
    <%= command_box("Search", "search") %>
    <div class="command_box">
       <%= image_tag("/images/icons/email_to.png")%>
       <%= command(:label => "Email this list", :command => "email #{@list.label} to")%>
     </div>
  <% end -%>
    

<% end -%>
