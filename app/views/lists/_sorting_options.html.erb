<dl class='tabs'>
      <dt>Sort</dt>
      <% options.each do |option| -%>
        <dd class=" <%= selected == option[1] ? 'current' : '' %>">
          <%= link_to_list(source, :order => option[1], :title => "By #{option[0]}") %>
        
        </dd>
      <% end -%>
    </dl>