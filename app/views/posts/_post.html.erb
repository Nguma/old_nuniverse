<% div_for :post, :class => "step" do -%>
  <h2><%= link_to_unless_current h(post.title), post %></h2>
  <strong><%= simple_format h(post.body) %></strong> <%= time_ago_in_words(post.created_at)%> ago.
<% end -%>