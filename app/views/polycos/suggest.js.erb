<% @suggestions.each do |suggestion| -%>
  <dd class="input_suggestion"><%= link_to thumbnail_tag(suggestion, :size => :small), '#', :title => "##{suggestion.unique_name}" rescue nil%><%= link_to suggestion.name, '#', :title => "##{suggestion.unique_name}"%></dd>
<% end -%>