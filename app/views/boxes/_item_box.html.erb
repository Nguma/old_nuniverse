<div class="box <%= item_classes rescue "" %>" >
  
  
  <%= render_item(item, :kind => item.kind, :item_classes => item_classes) %>
  <div class="content">
    
  </div>
  <div class="hidden">
    <%= link_to("",preview_url(item.object.id, :kind => item.kind), :class => "preview_url")%>
    <%= link_to("",add_to_nuniverse_url(@tag, item.kind, :tag => item.object.id, :label => item.object.label, :url => item.object.url, :kind => item.object.kind, :data => item.object.data, :description => item.object.description, :service => item.object.service ), :class => "add_to_fav_url")%>
    
    <%= link_to("Edit connections with #{@tag.label}",categorize_url(item.object.id, @tag), :class => "categorize_url")%>
  </div>
  <div class="delimiter">&nbsp;</div>
</div>