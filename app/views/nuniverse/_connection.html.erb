<dd class="connection <%= cycle('even', 'odd') %>">
  <div class="manage">
    <%= link_to(image_tag("/images/icons/minus.png"),"#",:title => "Remove") %>
  </div>
  <%= avatar_for(object) %>
  <h3>
  <%= link_to(object.content.capitalize,"/section_of/#{path}_#{object.id}", :class => "main inner") %></h3>
  <p><%= object.description %></p>

</dd>