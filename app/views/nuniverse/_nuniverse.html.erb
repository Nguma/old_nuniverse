<!-- <% path.taggings.each do |tagging| -%>
        <% if tagging.last_tag == path.last_tag -%>
          <% page_for(:tag => tagging.last_tag, :path => path) do -%> 
            <%= content_for_page(:tag => tagging.last_tag, :path => path, :perspective => perspective, :filter => filter) %>
        <% end -%>
        <% else -%>
          <% page_for(:tag => tagging.last_tag, :path => tagging) do -%>

            
          <% end -%> 
        <% end -%>

      <% end -%>   -->