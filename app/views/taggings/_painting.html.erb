<div class="column">
  <%= render :partial => "/tags/profile", :locals => {:tag => @tagging.object}%>
  <%= render :partial => "contributors", :locals => {:contributors => @contributors}%>
  <%= render :partial => "ads" %>
  <%= render :partial => "options", :locals => {:tagging => @tagging }%>
</div>
<div class="column" style="width:50%">
  <%= render :partial => "image", :locals => {:object => @tagging.object}%>
  <%= box_tag(:items => @tagging.connections(:kind => 'fact'), :title => "Factsheet", :toggle => "#fact ")%>
   <%= box_tag(:items => @tagging.connections(:kind => 'comments'), :title => "Comments", :toggle => "#comment ")%>
</div>

<div class="column">
  <%= render :partial => "map", :locals => {:tagging => @tagging}%>
  <%= box_tag(:items => @tagging.connections(:kind => 'location'), :title => "Where is it?", :toggle => "#location ")%>
  <%= render :partial => "bookmarks", :locals => {:tagging => @tagging}%>
</div>