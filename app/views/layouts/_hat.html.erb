
<% if logged_in? %>

    <% menu = [
      #link_to("Home", home_url(:kind => "nuniverse")),
     # link_to("About", about_url),

      link_to("Log out", logout_url)
      ] -%>
<% else -%>
<% menu = [
  link_to("About", about_url),

  link_to("Login", login_url),
  link_to("Register", signup_url)
  ] -%>
<% end -%>


<dl style="position:absolute;">
  <% menu.each do |item| -%>
    <dd><%= item %></dd>
  <% end -%>
</dl>