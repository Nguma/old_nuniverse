<div class="step">
   <h3>Where's that?</h3>
   <% @map = map(Location.gather(@story.nuniverses)) %>
   <% if @map -%>
      <% content_for :head do -%>
        <%= GMap.header (:large_map => true, :with_vml => false) %>
        <%= @map.to_html %>
      <% end -%>
      <%= @map.div(:width => "100%", :height => 300, :style=>"position:relative") %>

   <% end -%>
 </div>