<% render_section(:html => {:id => "S-stories-for-#{@source.class.to_s}-#{@source.id}", :class => "main-section"}) do %>
  <h2>Your nuniverses</h2>

  <% @stories.each do |story| -%>
    <%= render :partial => "/stories/instance", :locals => {:instance => story} %>
  <% end -%>

<% end -%>

