   <%= link_to "Add a nuniverse", "#", :class => "expand_lnk", :id => "expand_connect_nuniverse_form" %>
  
  <% @new_polyco = Polyco.new(:object => @source, :subject => Image.new)  %>
  <% form_for @new_polyco, :html => {:multipart => true, :id => "connect_nuniverse_form"} do |f| -%>
    
    <%= f.hidden_field :object_id %>
    <%= f.hidden_field :object_type %>
    
    <% fields_for(:subject) do |s| %>   
       <%= s.text_field :name , :id => "input_visit"%><%= f.submit "Visit", :name => "submit"  %> or <%= f.submit "Connect.", :name => "submit" %>
    <% end -%>
    
    
  <% end -%>
