<div id="facts" class="section">
  <h2>Quick Facts</h2>
  <%= render :partial => "/nuniverse/search" , :locals => {:action => facts_url}%>
  <div class="content">
    <div class="step">
      <%= will_paginate @facts unless @facts.empty?%>
      <%= clear %>
    </div>
    <% @nuniverse.facts.each do |fact| -%>
      <%= render :partial => "/facts/instance", :locals => {:instance => fact} %>
    <% end -%>
    <%= clear %>
    
    <%= render :partial => "/facts/new" %>
    <%= clear %>
    To add a fact, just type it in. Wrap the name of the nuniverses you want to connect with like this: <span style="font-weight:bold">[[Nuniverse Name]]</span> 
  </div>
</div>




