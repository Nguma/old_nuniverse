
<% @comments.each do |comment| -%>
  <% vote = @namespace.votes.by(comment.author).first %>
  <div class="box" style="background:<%= color_for_score(vote.score) %>"><%= render :partial => comment %></div>
<% end -%>