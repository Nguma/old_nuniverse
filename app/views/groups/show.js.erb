<h2><%= @group.name %></h2>
<% @elements.each do |connection| -%>
    <%= render :partial => "/nuniverses/instance", :locals => {
      :instance => connection.subject,
      :properties => @group.properties
    } %>
 <% end -%>

 <ul class="sub-menu">
   <li><%= link_to "Add an item", "/add-item-to-set/#{@group.id}", :class => "expand-lnk", :id => "expand-item-form" %></li>
      <li><%= link_to "Edit this set", "/groups/edit/#{@group.id}", :class => "expand-lnk", :id => "expand-item-form" %></li>
  </ul>